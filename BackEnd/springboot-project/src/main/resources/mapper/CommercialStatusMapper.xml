<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aladin.openData.mapper.CommercialStatusMapper">
    <!-- 특정 구 코드로 조회 -->
    <select id="findOneByGuCode" parameterType="String" resultType="com.aladin.openData.vo.CommercialStatusVO">
        SELECT 
            code, gugun_name AS gugunName, business_change_indicator AS businessChangeIndicator,
            cctv, cctv_rank AS cctvRank, 
            total_floating_population AS totalFloatingPopulation, floating_population_rank AS floatingPopulationRank,
            total_residential_population AS totalResidentialPopulation, residential_population_rank AS residentialPopulationRank,
            avg_apartment_price AS avgApartmentPrice, apartment_price_rank AS apartmentPriceRank,
            avg_apartment_complexes AS avgApartmentComplexes, apartment_complexes_rank AS apartmentComplexesRank,
            avg_working_population AS avgWorkingPopulation, working_population_rank AS workingPopulationRank
        FROM seoul_commercial_status
        WHERE code = #{guCode}
    </select>

    <!-- 전체 목록 조회 -->
    <select id="findAll" resultType="com.aladin.openData.vo.CommercialStatusVO">
        SELECT 
            code, gugun_name AS gugunName, business_change_indicator AS businessChangeIndicator,
            cctv, cctv_rank AS cctvRank, 
            total_floating_population AS totalFloatingPopulation, floating_population_rank AS floatingPopulationRank,
            total_residential_population AS totalResidentialPopulation, residential_population_rank AS residentialPopulationRank,
            avg_apartment_price AS avgApartmentPrice, apartment_price_rank AS apartmentPriceRank,
            avg_apartment_complexes AS avgApartmentComplexes, apartment_complexes_rank AS apartmentComplexesRank,
            avg_working_population AS avgWorkingPopulation, working_population_rank AS workingPopulationRank
        FROM seoul_commercial_status
    </select>
</mapper>
