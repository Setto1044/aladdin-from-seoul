<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aladin.comment.mapper.CommentMapper">

	<!-- ResultMap 정의 -->
	<resultMap id="commentResultMap" type="com.aladin.comment.dto.CommentResponseDto">
		<id property="id" column="id" />
		<result property="roomboardsId" column="roomboards_id" />
		<result property="membersUsername" column="members_username" />
		<result property="comment" column="comment" />
		<result property="createdAt" column="created_at" />
		<result property="modifiedAt" column="modified_at" />
	</resultMap>

	<!-- 댓글 등록 -->
	<insert id="insertComment" parameterType="com.aladin.comment.dto.CommentRequestDto">
		INSERT INTO roomboards_comments (roomboards_id, members_username, comment, created_at, modified_at)
		VALUES (#{roomboardsId}, #{membersUsername}, #{comment}, NOW(), NOW());
	</insert>

	<!-- 댓글 수정 -->
	<update id="updateComment" parameterType="map">
		UPDATE roomboards_comments
		SET comment = #{comment}, modified_at = NOW()
		WHERE id = #{id};
	</update>

	<!-- 댓글 삭제 -->
	<delete id="deleteComment" parameterType="long">
		DELETE FROM roomboards_comments WHERE id = #{id};
	</delete>

	<!-- 댓글 ID로 댓글 1개 조회 -->
	<select id="findCommentById" resultMap="commentResultMap">
		SELECT
		id,
		roomboards_id,
		members_username,
		comment,
		created_at,
		modified_at
		FROM roomboards_comments
		WHERE id = #{id};
	</select>

	<!-- 특정 게시물의 댓글 조회 (커서 기반 페이징) -->
	<select id="findCommentsByRoomBoardId" resultMap="commentResultMap">
		SELECT
		id,
		roomboards_id,
		members_username,
		comment,
		created_at,
		modified_at
		FROM roomboards_comments
		WHERE roomboards_id = #{roomboardsId}
		<if test="cursorId != null">
			AND id &gt; #{cursorId}
		</if>
		ORDER BY id ASC
		LIMIT #{pageSize};
	</select>

</mapper>
